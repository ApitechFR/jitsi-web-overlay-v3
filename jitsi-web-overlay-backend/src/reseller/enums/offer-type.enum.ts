/**
 * OfferType enum
 * - BASIC: Modules core (visio, feedback)
 * - PREMIUM: Modules étendue (webinar, replay, recording, voxify) + personnalisation activée
 */
export enum OfferType {
    BASIC = 'basic',
    PREMIUM = 'premium',
}
